# Configuration des variables d'environnement
# Copiez ce fichier en .env et adaptez les valeurs pour votre environnement

# Clé secrète Django (générez une nouvelle clé pour la production)
SECRET_KEY=django-insecure-%@0=w0ugu_i+%=jp9r=ms@))%kcy=e5pz%j&*ufji9-5&lm^00

# Mode debug (False pour la production)
DEBUG=True

# Hôtes autorisés (séparez par des virgules)
ALLOWED_HOSTS=localhost,127.0.0.1

# Configuration de la base de données
# Pour PostgreSQL (recommandé en production):
# DATABASE_URL=postgresql://user:password@localhost:5432/db_name

# Pour SQLite (développement uniquement):
DATABASE_URL=sqlite:///db.sqlite3

# Configuration du cache Redis (optionnel)
# REDIS_URL=redis://localhost:6379/0

# Configuration email (optionnel)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=votre_email@gmail.com
# EMAIL_HOST_PASSWORD=votre_mot_de_passe